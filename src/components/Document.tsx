"use client";
function Document({ id }: { id: string }) {
  return (
    <div>
      <div>
        <form action="">
          {/* upate title */}

          {/* IF */}
          {/* isOwner && Inviteuser,DeleteDocument */}
        </form>
      </div>
      <div>
        {/*  Manage User*/}

        {/* Avatar */}
      </div>

      {/* Collaborate editor */}
    </div>
  );
}

export default Document;
